---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Eric Alex | Home">
  <main>
    <section class="hero-section">
      <div class="slideshow-container">
        <div class="slide-text" id="slideText"></div>
      </div>
    </section>

    <section class="about-me-section">
      <div class="about-me-header">
        <p class="section-label">About Me</p>
        <div class="social-links-about">
          <a href="#"><img src="/assets/linkedin.svg" alt="LinkedIn" /></a>
          <a href="https://www.youtube.com/@eric10alex"><img src="/assets/youtube.svg" alt="Youtube" /></a>
          <a href="https://www.instagram.com/ericc_jpg/"><img src="/assets/instagram.svg" alt="Instagram" /></a>
          <a href="https://x.com/eric14alex"><img src="/assets/twitter.svg" alt="Twitter" /></a>
          <a href="mailto:eric14alex@gmail.com"><img src="/assets/email.svg" alt="Email" /></a>
        </div>
      </div>
      <div class="about-me-content">
        <div class="text-content">
          <p>
            i work in corporate during the day, handling business, clients, and getting leads. it’s structured, fast, and keeps me sharp with how things actually move in the real world.
          </p>
          <p>
once that part's done, i switch to building projects, shooting photos, creating videos, and designing things. that balance keeps me grounded.
          </p>
        </div>
        <div class="image-wrapper">
          <img src="/assets/eric.png" alt="Eric" class="profile-image" />
        </div>
      </div>
       <dev class="snippets-section">
      <div class="snippet-item">
        <p class="snippet-label">Currently Working At</p>
        <h4 class="snippet-content">Tailwebs Technology</h4>
      </div>
      <div class="snippet-item">
        <p class="snippet-label">Interests</p>
        <h4 class="snippet-content">Creative Stuff, Trekking, Gaming, Travelling, Football</h4>
      </div>
      <div class="snippet-item">
        <p class="snippet-label">Quote</p>
        <h4 class="snippet-content">"Full send"</h4>
      </div>
    </dev>
    </section>

    <section class="skills-section">
      <div class="skills-header">
        <p class="section-label">What I Do And How?</p>
      </div>
      
      <h2 class="skills-main-title">
       i build, create, and figure things out as i go. that's pretty much how everything starts.
      </h2>

      <div class="skills-grid">
        <div class="skill-card">
          <div class="skill-icon-wrapper"><span>M</span></div>
          <h3 class="skill-title">marketing</h3>
          <p class="skill-description">
            i like figuring out what makes people connect and what helps things grow. it's not just content, it's about building something real that scales naturally.
          </p>
        </div>
        <div class="skill-card">
          <div class="skill-icon-wrapper"><span>D</span></div>
          <h3 class="skill-title">design</h3>
          <p class="skill-description">
          i like starting with a blank canvas and building something that slowly starts to make sense. seeing random shapes and colors turn into a finished design is the best part.
          </p>
        </div>
        <div class="skill-card">
          <div class="skill-icon-wrapper"><span>V</span></div>
          <h3 class="skill-title">videography</h3>
          <p class="skill-description">
            turning still moments into motion feels different. i like building small stories that feel real, nothing forced, just clean visuals that pull you in.
          </p>
        </div>
        <div class="skill-card">
          <div class="skill-icon-wrapper"><span>P</span></div>
          <h3 class="skill-title">photography</h3>
          <p class="skill-description">
            there's something about freezing a moment that'll never happen again. could be a sunrise, a random road, or a mountain view.
          </p>
        </div>
      </div>
    </section>

  </main>
</Layout>

<script>
  // Define slides for both desktop and mobile
  const slidesDesktop = [
    { image: "/assets/img8.jpg", title: "The Houseboat" },
    { image: "/assets/img6.jpg", title: "The Landscape at Chikkamagaluru" },
  ];

  const slidesMobile = [
    { image: "/assets/img7.jpg", title: "MRF Motorcross" },
    { image: "/assets/img3.jpg", title: "The Sunset at Goa" },
  ];

  // Check if device is mobile
  const isMobile = window.innerWidth <= 768;
  const slides = isMobile ? slidesMobile : slidesDesktop;

  document.addEventListener('DOMContentLoaded', () => {
    const container = document.querySelector(".slideshow-container") as HTMLDivElement;
    const textBox = document.getElementById("slideText");

    if (container && textBox) {
      let currentIndex = 0;

      // Function to show a specific slide
      function showSlide(index: number): void {
        const { image, title } = slides[index];
        container.style.backgroundImage = `url('${image}')`;
        textBox.textContent = title;
      }

      // Initial slide
      showSlide(currentIndex);

      // Automatically switch slides every 4 seconds
      const slideInterval = setInterval(() => {
        currentIndex = (currentIndex + 1) % slides.length;
        showSlide(currentIndex);
      }, 6000);
    }
  });
</script>


<style>
  /* Existing Hero Section Styles */
  .hero-section {
    width: 100%;
    padding: 2rem 0;
    background-color: var(--background-color);
  }

  .slideshow-container {
    position: relative;
    width: 95%;
    height: 80vh;
    margin: 0 auto;
    background-color: black;
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center bottom;
    border: 2px solid #333333;
    border-radius: 10px;
    overflow: hidden;
    transition: background-image 2s ease-in-out;
  }

  .slide-text {
    position: absolute;
    bottom: -0.1rem;
    right: 0rem;
    background: rgba(26, 26, 26, 1);
    color: var(--text-color);
    padding: 0.6rem 1.2rem;
    border-radius: 8px;
    font-size: 1rem;
    font-family: 'Inter', sans-serif;
    letter-spacing: 0.5px;
  }

  /* Content section styling (was content-section, now more specific about-me-section) */
  .content-section { /* Kept in case you had other content planned for it */
    padding: 6rem 2rem;
    text-align: center;
    background-color: var(--background-color);
    min-height: 50vh;
  }

  /* Mobile adjustments for hero */
  @media (max-width: 768px) {
    .slideshow-container {
      width: 90%;
      height: 80vh;
      border-width: 4px;
    }
    .slide-text {
      font-size: 1rem;
      padding: 0.5rem 1rem;
      bottom: 0rem;
      right: -0.1rem;
    }
  }

  /* ================================== */
  /* ABOUT ME SECTION STYLES (BOXED) */
  /* ================================== */
  .about-me-section {
    width: 95%;
    margin: 4rem auto; 
    padding: 2rem 2.5rem; 
    border: 2px solid rgba(51, 51, 51, 0.2); 
    border-radius: 10px; 
    background-color: var(--background-color);
    display: flex;
    flex-direction: column;
    gap: 2rem; 
  }

  .about-me-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 1.5rem;
  }

  .section-label {
    margin: 0;
    font-size: 0.9rem;
    color: var(--light-text-color);
    letter-spacing: 0.05em;
  }

  .social-links-about {
    display: flex;
    gap: 1.2rem;
  }

  .social-links-about img {
    width: 20px;
    height: 20px;
    filter: brightness(0) invert(1) opacity(0.6);
    transition: opacity 0.3s ease;
  }

  .social-links-about img:hover {
    opacity: 1;
  }

  .about-me-content {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 3rem;
  }

  .text-content {
    flex: 2;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .text-content p {
    margin: 0;
    font-size: 1.2rem;
    line-height: 1.6;
    color: var(--text-color);
  }

  .image-wrapper {
    flex: 1;
    display: flex;
    justify-content: flex-end;
    position: relative;
    padding-top: 1.5rem;
  }

  .profile-image {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    object-fit: cover;
    border: 1px solid var(--border-color);
    background-color: black;
  }

  /* ================================== */
  /* Experience Grid (MODIFIED)         */
  /* ================================== */
  .experience-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0; 
    border-top: 1px solid var(--border-color);
    padding-top: 2rem;
  }

  .experience-item {
    padding: 1rem;
    text-align: center;
  }

  /* NEW: Add vertical divider on desktop */
  .experience-item:not(:last-child) {
    border-right: 1px solid var(--border-color);
  }

  .experience-item h4 {
    margin: 0 0 0.4rem 0;
    font-size: 1rem;
    font-weight: 500;
    color: var(--text-color);
  }

  .experience-item p {
    margin: 0;
    font-size: 0.8rem;
    color: var(--light-text-color);
  }

  /* Mobile adjustments for About Me */
  @media (max-width: 768px) {
    .about-me-section {
      width: 90%; 
      padding: 1.5rem;
      gap: 1.5rem;
    }

    .about-me-header {
      flex-direction: column;
      align-items: center; 
      gap: 1rem;
      padding-bottom: 1rem;
    }

    .social-links-about {
      justify-content: center; 
      width: 100%;
    }

    .about-me-content {
      flex-direction: column; 
      align-items: center;
      gap: 2rem;
    }

    .text-content {
      text-align: center; 
      gap: 1rem;
    }

  .text-content p {
      font-size: 1rem;
    }

    .image-wrapper {
      justify-content: center; 
      padding-top: 0;
    }

    .profile-image {
      width: 200px; 
      height: 200px;
    }

    /* MODIFIED: Experience Grid on Mobile */
    .experience-grid {
      grid-template-columns: 1fr;
      gap: 0; 
      padding-top: 1.5rem;
    }

    /* MODIFIED: Experience Item on Mobile */
    .experience-item {
      padding: 1.5rem 0; 
    }

    /* MODIFIED: Divider rules for mobile */
    .experience-item:not(:last-child) {
      border-right: none; 
      border-bottom: 1px solid var(--border-color); 
    }
  }

  /* ================================== */
  /* NEW SNIPPETS SECTION STYLES (Added)*/
  /* ================================== */
  .snippets-section {
    width: 95%;
    margin: 4rem auto 0; /* Space above the skills section */
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    border-top: 1px solid var(--border-color);
    padding-top: 2rem;
  }

  .snippet-item {
    padding: 1rem;
    text-align: center;
  }

  /* Vertical divider for desktop */
  .snippet-item:not(:last-child) {
    border-right: 1px solid var(--border-color);
  }

  .snippet-label {
    margin: 0 0 0.5rem 0;
    font-size: 0.8rem;
    color: var(--light-text-color);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .snippet-content {
    margin: 0;
    font-size: 1rem;
    font-weight: 500;
    color: var(--text-color);
    line-height: 1.5;
  }

  /* Mobile adjustments for snippets */
  @media (max-width: 768px) {
    .snippets-section {
      grid-template-columns: 1fr;
      gap: 0;
      padding-top: 1.5rem;
      margin: 3rem auto 0;
    }

    .snippet-item {
      padding: 1.5rem 0;
    }

    /* Horizontal divider for mobile */
    .snippet-item:not(:last-child) {
      border-right: none;
      border-bottom: 1px solid var(--border-color);
    }
  }

  /* ================================== */
  /* SKILLS SECTION STYLES (Unchanged)  */
  /* ================================== */
  .skills-section {
    width: 95%;
    margin: 4rem auto;
    padding: 2rem 2.5rem; 
    border: 2px solid rgba(51, 51, 51, 0.2);
    border-radius: 10px; 
    background-color: var(--background-color);
    display: flex;
    flex-direction: column;
    gap: 2rem; 
  }

  .skills-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 1.5rem;
  }

  .skills-main-title {
    font-size: 2.5rem; 
    font-weight: 500;
    color: var(--text-color);
    line-height: 1.4;
    max-width: 100%; 
    margin: 0 0 1rem 0; 
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(4, 2fr); 
    gap: 1.5rem;
  }

  .skill-card {
    display: flex;
    flex-direction: column;
    gap: 1.2rem; 
    padding: 2rem 1.5rem;
    border: 1px solid var(--border-color);
    border-radius: 10px;
    background-color: rgba(26, 26, 26, 0.3); 
    transition: background-color 0.3s ease, border-color 0.3s ease;
  }

  .skill-card:hover {
    background-color: rgba(40, 40, 40, 0.5);
    border-color: #555;
  }

  .skill-icon-wrapper {
    width: 40px;
    height: 40px;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--light-text-color);
    background-color: rgba(40, 40, 40, 0.5);
  }

  .skill-title {
    font-size: 1.2rem;
    font-weight: 500;
    color: var(--text-color);
    text-transform: capitalize;
    margin: 0;
  }

  .skill-description {
    font-size: 0.9rem;
    color: var(--light-text-color);
    line-height: 1.6;
    margin: 0;
  }

  /* Mobile adjustments for Skills Section */
  @media (max-width: 1024px) {
    .skills-main-title {
      font-size: 2rem;
      max-width: 100%;
    }
    .skills-grid {
      grid-template-columns: repeat(2, 1fr); 
    }
  }

  @media (max-width: 768px) {
    .skills-section {
      width: 90%;
      padding: 1.5rem; 
      gap: 1.5rem;
    }
    
    .skills-header {
      padding-bottom: 1rem;
      justify-content: center; 
    }

    .skills-main-title {
      font-size: 1.8rem;
      text-align: center; 
    }

    .skills-grid {
      grid-template-columns: 1fr; 
    }

    .skill-card {
      padding: 1.5rem;
    }
  }

</style>